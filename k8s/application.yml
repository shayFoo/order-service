spring:
  security:
    oauth2:
      client:
        provider:
          keycloak:
            issuer-uri: http://polar-keycloak/realms/PolarBookshop
  r2dbc:
    url: r2dbc:postgresql://polar-postgres/polardb_order
  flyway:
    url: jdbc:postgresql://polar-postgres/polardb_order
  rabbitmq:
    host: polar-rabbitmq
polar:
  catalog-service-uri: http://catalog-service

